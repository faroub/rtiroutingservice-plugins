# How to build all available libraries 
cd rtiroutingservice-plugins

rm -rf build
mkdir build
cd build

cmake .. \
  -DCMAKE_C_FLAGS="-fcommon" \
  -DCMAKE_CXX_FLAGS="-fcommon"

cmake --build . -j


